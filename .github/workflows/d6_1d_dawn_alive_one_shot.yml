name: D6.1d dawn alive one-shot

on:
  push:
    branches:
      - codex/d6-root-reaudit
    paths:
      - .github/workflows/d6_1d_dawn_alive_one_shot.yml

permissions:
  contents: write

jobs:
  patch-test-push-cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v6
        with:
          ref: codex/d6-root-reaudit
          fetch-depth: 0

      - name: Verify exact baseline and tooling scope
        shell: bash
        run: |
          set -euo pipefail
          BASE_PRODUCT='f8328639215c3836d74849bed5ca57d9024087db'
          APP='app/src/main/java/com/codex/campboardgamehost/CampBoardGameHostApp.kt'
          EXPECTED_APP_BLOB='df9e9f227719469605413b9d4bd80e3538065be7'
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test "$(git ls-remote origin refs/heads/codex/d6-root-reaudit | cut -f1)" = "$GITHUB_SHA"
          git merge-base --is-ancestor "$BASE_PRODUCT" HEAD
          test "$(git hash-object "$APP")" = "$EXPECTED_APP_BLOB"
          actual="$(git diff --name-only "$BASE_PRODUCT"..HEAD | sort)"
          expected="$(printf '%s\n' \
            '.github/scripts/d6_1d_dawn_alive_patch.py' \
            '.github/workflows/d6_1d_dawn_alive_one_shot.yml' | sort)"
          test "$actual" = "$expected"

      - name: Apply bounded large-file patch
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/d6_1d_dawn_alive_patch.py

      - name: Exact product diff audit
        shell: bash
        run: |
          set -euo pipefail
          APP='app/src/main/java/com/codex/campboardgamehost/CampBoardGameHostApp.kt'
          test "$(git diff --name-only)" = "$APP"
          git diff --check
          test "$(grep -c 'synchronizePlayerDeathWithinCurrentRevision(' "$APP")" -eq 4
          git diff -- "$APP"

      - name: Focused dawn ownership tests
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          ./gradlew :app:testDebugUnitTest \
            --tests 'com.codex.campboardgamehost.clocktower.session.ClocktowerGameSessionAliveBoundaryTest' \
            --tests 'com.codex.campboardgamehost.clocktower.session.NightDawnDurableMaterializationPlannerTest' \
            --tests 'com.codex.campboardgamehost.clocktower.session.NightDawnResolutionPlannerMayorContractTest' \
            --tests 'com.codex.campboardgamehost.clocktower.session.NightDawnResolutionPlannerSuccessionResolutionTest' \
            --tests 'com.codex.campboardgamehost.clocktower.session.NightDawnRestoreRetryConvergenceAcceptanceTest' \
            --no-daemon

      - name: Android FAST
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew :app:testFast --no-daemon --build-cache

      - name: Commit and push product patch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java/com/codex/campboardgamehost/CampBoardGameHostApp.kt
          git commit -m 'refactor: sync dawn death to Clocktower session'
          git push origin HEAD:codex/d6-root-reaudit

      - name: Remove one-shot tooling
        shell: bash
        run: |
          set -euo pipefail
          git rm \
            .github/scripts/d6_1d_dawn_alive_patch.py \
            .github/workflows/d6_1d_dawn_alive_one_shot.yml
          git commit -m 'chore: remove D6 dawn alive one-shot tooling'
          git push origin HEAD:codex/d6-root-reaudit
