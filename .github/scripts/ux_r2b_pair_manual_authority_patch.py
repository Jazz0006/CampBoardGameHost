from pathlib import Path

# Placeholder created to reserve the fail-closed one-shot patch path.
# The exact locked anchors are filled only after the current PR head and target blobs are re-read.
raise SystemExit("UX-R2B patch script not armed: exact anchors not yet locked")
