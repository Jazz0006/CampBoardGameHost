# SDE-3B — BEGINNER_CONSERVATIVE_V1 Completion Audit

> Date: 2026-09-24 Australia/Sydney  
> Branch: `sde-3b-beginner-conservative-v1`  
> PR: **#153 — draft**  
> Status: **COMPLETE — T4 ACCEPTED; PR REMAINS DRAFT**

## 1. Completion conclusion

SDE-3B is complete for the current engineering and evidence checkpoint.

The accepted `BEGINNER_CONSERVATIVE_V1` implementation establishes a conservative, interpretable, score-free policy boundary over production-owned legality and exact semantic consequences while preserving explicit uncertainty where evidence is insufficient.

The accepted policy shape is:

`legal candidates -> exact no-credible-Evil-world contradiction gate -> survivor equivalence band -> seeded survivor-only selection`

No unsupported soft ordering was invented.

## 2. Completed slices

- **3B1 historical lifecycle / input binding — COMPLETE**
  - lifecycle-safe committed-prefix binding;
  - canonical history ownership preserved;
  - no-hindsight enforced;
  - external contextual owners referenced rather than copied.

- **3B2 confirmation-chain projection — COMPLETE**
  - recipient-visible committed history;
  - exact leave-one-out support/contradiction/independence;
  - source-authentication provenance;
  - no policy preference inferred from semantic projection alone.

- **3B3 impaired-narrative projection — COMPLETE**
  - persistent setup-bound versus temporary action-bound impairment;
  - role-agnostic historical narrative projection;
  - forced/avoidable transition and detectability facts;
  - no second mutable perceived-world store.

- **3B4 healthy-information utility — COMPLETE**
  - usable/independent whole-table information routes;
  - redundancy, contradiction, route loss and last-route removal;
  - legal registered truth remains healthy when the ability is functioning;
  - no fixed information budget or numeric floor.

- **3B5 contextual role-function exposure — COMPLETE**
  - generic direct/new/already/confirmation-amplified exposure;
  - forced versus avoidable exposure;
  - registration ambiguity as the first rules-backed mechanism;
  - canonical history and existing confirmation provenance reused;
  - severity remains evidence-gated.

- **3B6 expert-informed V1 soft-priority eligibility — COMPLETE**
  - E3 audit found no additional soft ordering currently authorized;
  - `preference-evidence-not-authorized` is explicit in Ready policy results;
  - unsupported dimensions remain tied rather than silently scored;
  - no new policy reason was introduced.

## 3. Accepted V1 invariants

The final SDE-3B implementation preserves:

- rules/canonical producers as legality authority;
- exact semantic/topology owners as consequence authority;
- SDE as policy/selection authority only;
- exact zero Evil-topology retention as the only generic hard rejection;
- no rejection of non-zero topology retention through an invented threshold;
- explicit deferral when required strategic evidence is unavailable or undefined;
- explicit survivor equivalence when evidence cannot distinguish candidates;
- deterministic, order-independent seeded selection among survivors;
- no legacy score/probability/pressure import into SDE policy;
- no opaque global scalar;
- no named-role policy patches;
- no production recommendation cutover;
- no DecisionTrace persistence in SDE-3B.

## 4. T4 acceptance checkpoint

T4 checkpoint head:

`30caec3dcd546f4395238809d1f1d285688cd814`

Commit:

`[full-ci] SDE-3B acceptance checkpoint`

Remote acceptance:

- CI #3427 / run `35949251136`: **success**;
- R2 #3183 / run `35949251146`: **success**.

CI #3427 full-checkpoint evidence:

- change classification: success;
- full Android JVM regression + debug APK: success;
- ASP golden-corpus validation: success;
- ASP Oracle harness unit tests: success;
- Real Clingo cross-validation: success;
- aggregate CI gate: success.

The full checkpoint intentionally skipped Android FAST because the full Android path was selected instead.

## 5. Final fanout / scope audit

Compared with merged `main`:

`2a9051f1b0282bd25d01d46d36fe797857cc429d`

the accepted T4 head is strictly ahead with no main divergence.

The SDE-3B fanout is concentrated in:

- SDE policy / selector contracts;
- structured decision feature projection;
- lifecycle/history/context projectors;
- confirmation, impaired-narrative, healthy-information and role-function-exposure feature families;
- bounded rules support required to preserve authoritative ability functioning semantics;
- corresponding typed unit/integration/evidence regression tests;
- SDE route, audit and handoff documentation.

The audit found no production-cutover, persistence-schema, UI recommendation-authority, global scoring, or unsupported named-role preference surface added by SDE-3B.

## 6. Evidence boundary preserved

SDE-3B intentionally stops before unsupported calibration.

Still deferred:

- numeric healthy-information thresholds;
- role-function-exposure severity;
- cross-expert impaired-narrative preference strength;
- Demon-bluff triplet ordering;
- quantitative multi-axis tradeoffs;
- automatic production cutover.

Those remain evidence/versioning concerns for later phases rather than hidden constants in V1.

## 7. PR governance

PR #153 remains:

- open;
- draft;
- unmerged.

Do not mark ready or merge unless the project owner explicitly says:

`授权合并`

Completion of SDE-3B and its T4 gate does not override that governance boundary.

## 8. Next engineering phase

The next engineering phase is **SDE-3C — DecisionTrace / shadow replay**.

SDE-3C should begin with an architecture/fanout audit before persistence edits and should own:

- versioned policy identity;
- complete legal candidate IDs;
- typed feature snapshots;
- policy rejection/survival reasons;
- shadow recommendation;
- actual committed choice;
- manual override flag/reason;
- replay of the same committed history under multiple policy versions.

SDE-3C must not mutate historical truth or silently convert V1 into production authority.
