# MS-S1 CI Trigger Note

MS-S1 uses the normal campaign Draft PR to obtain GitHub CI because `ci.yml` triggers on pull requests to `main` but not ordinary feature-branch pushes.

This note contains no product requirement and may be removed during later documentation cleanup.
